meta {
  name: Block Chat Session
  type: http
  seq: 7
}

patch {
  url: {{baseUrl}}/api/v1/chat-sessions/{{chatSessionId}}/block
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  # Block Chat Session

  Blocks a chat session from further activity.
}
