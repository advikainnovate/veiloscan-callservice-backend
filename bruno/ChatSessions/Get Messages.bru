meta {
  name: Get Room Messages
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/chat-sessions/rooms/{{roomId}}/messages
  body: none
  auth: none
}

headers {
  x-api-key: {{apiKey}}
}

params:query {
  limit: 50
  before: 
}

docs {
  # Get Room Messages

  Returns messages in ascending order (oldest first).
  Supports pagination via query params.

  ## Query params
  - `limit`  — number of messages to return (default: 50)
  - `before` — ISO timestamp — returns messages older than this (for infinite scroll / load more)

  ## Message status values
  sent → delivered → read → failed
}
