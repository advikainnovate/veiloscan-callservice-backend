meta {
  name: Get Active Chat Sessions
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/chat-sessions/active/list
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  # Get Active Chat Sessions

  Returns currently active chat sessions.
}
