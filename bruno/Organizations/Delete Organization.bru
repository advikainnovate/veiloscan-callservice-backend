meta {
  name: Delete Organization
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/organizations/:id
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  # Delete Organization
}