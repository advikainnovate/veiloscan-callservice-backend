meta {
  name: Revoke QR Code
  type: http
  seq: 10
}

patch {
  url: {{baseUrl}}/api/v1/qr-codes/{{qrCodeId}}/revoke
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  # Revoke QR Code

  Revokes an active QR code. Requires admin auth.
}
